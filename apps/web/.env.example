# 999x Web — Environment Variables
# Copy to .env and set for your environment

# API URL — in dev uses proxy (/api → localhost:3001), in prod set to your API domain
VITE_API_URL=http://localhost:3001
# For production:
# VITE_API_URL=https://api.999x.earth
